/**
 * Values the browser needs outside CSS, where `var(--token)` cannot be used
 * (e.g. `<meta name="theme-color">`). They mirror `styles/tokens.css`; a test keeps them equal.
 */
export const themeColor = '#06101d';
