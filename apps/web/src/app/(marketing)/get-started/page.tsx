import { PlaceholderPage } from '../../../components/marketing/placeholder-page';

export const metadata = { title: 'Get started' };

export default function GetStartedPage() {
  return (
    <PlaceholderPage eyebrow="Get started" title="Access isn’t open yet.">
      <p>
        LexGhana is still a platform preview, so there is nothing to sign up for. No form on this
        site collects your details. The demonstration workspace shows where the product is heading,
        using synthetic records only.
      </p>
    </PlaceholderPage>
  );
}
