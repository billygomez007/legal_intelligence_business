import { PlaceholderPage } from '../../../components/marketing/placeholder-page';

export const metadata = { title: 'Sign in' };

export default function SignInPage() {
  return (
    <PlaceholderPage eyebrow="Sign in" title="Sign-in isn’t available yet.">
      <p>
        LexGhana is a platform preview. There are no accounts, no passwords and nothing to sign in
        to. You can explore the workspace with synthetic data instead.
      </p>
    </PlaceholderPage>
  );
}
